
const add = (a, b) => a + b;
const sub = (a, b) => a - b;
const mult = (a, b) => a * b;
const div = (a, b) => a / b;

module.exports = {
    adicao: add,
    subtracao: sub,
    multiplicacao: mult,
    divisao: div
}